const { join , resolve } = require('path');
const { alias } = require('yargs');

module.exports = () =>{
    return {
        resolve : {
            alias : {
                'simple-color-picker' : resolve(join(__dirname,'..','node_modules','simple-color-picker'))
            }
        }
    }
}